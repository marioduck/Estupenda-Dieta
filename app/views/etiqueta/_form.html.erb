<%= form_for(@etiquetum) do |f| %>
  <% if @etiquetum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@etiquetum.errors.count, "error") %> prohibited this etiquetum from being saved:</h2>

      <ul>
      <% @etiquetum.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :descripcion %><br />
    <%= f.text_area :descripcion %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
