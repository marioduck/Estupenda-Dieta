<%= form_for(@medida) do |f| %>
  <% if @medida.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@medida.errors.count, "error") %> prohibited this medida from being saved:</h2>

      <ul>
      <% @medida.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
