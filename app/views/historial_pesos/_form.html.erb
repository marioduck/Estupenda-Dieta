<%= form_for(@historial_peso) do |f| %>
  <% if @historial_peso.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@historial_peso.errors.count, "error") %> prohibited this historial_peso from being saved:</h2>

      <ul>
      <% @historial_peso.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :peso_anterior %><br />
    <%= f.text_field :peso_anterior %>
  </div>
  <div class="field">
    <%= f.label :peso_actual %><br />
    <%= f.text_field :peso_actual %>
  </div>
  <div class="field">
    <%= f.label :fecha_registro %><br />
    <%= f.date_select :fecha_registro %>
  </div>
  <div class="field">
    <%= f.label :bmi %><br />
    <%= f.text_field :bmi %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
